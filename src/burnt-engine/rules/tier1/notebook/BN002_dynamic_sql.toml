# BN002 - Dynamic SQL (f-string interpolation) prevents accurate cost estimation
# Category: BasicNotebook
# Severity: warning
# Language: notebook
# Tier: 1

[rule]
id = "dynamic_sql"
code = "BN002"
severity = "warning"
language = "notebook"
description = "Dynamic SQL (f-string interpolation) prevents accurate cost estimation"
suggestion = "Use parameters or static SQL for better analysis"
category = "BasicNotebook"
tier = 1

[context]
type = "dynamic_sql_detection"
required = true
