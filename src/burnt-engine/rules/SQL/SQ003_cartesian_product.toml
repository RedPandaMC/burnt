# SQ003 - Cartesian product detected
# Category: SqlQuality
# Severity: error
# Language: sql

[rule]
id = "cartesian_product"
code = "SQ003"
severity = "error"
language = "sql"
description = "Cartesian product detected"
suggestion = "Add appropriate join condition"
category = "SqlQuality"

[query]
# CPL pattern - JOIN without ON clause
# Note: Full detection requires context analysis
cpl_detect = """
SELECT * FROM $a JOIN $b
"""
