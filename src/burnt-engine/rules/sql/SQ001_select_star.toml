# SQ001 - SELECT * without LIMIT (SQL context-aware)
## Rule: SQ001
## Description: SELECT * without LIMIT
## Severity: warning
## Category: SQL Quality
## Tags: ["sql", "performance", "select-star"]

[rule]
id = "select_star"
code = "SQ001"
severity = "warning"
language = "sql"
description = "SELECT * without LIMIT"
suggestion = "Add LIMIT or specify columns"
category = "SqlQuality"
tags = ["sql", "performance", "select-star"]
platform = "all"

[tests]
pass = [
    "SELECT id, name FROM users LIMIT 100",
    "SELECT * FROM users LIMIT 100",
    "SELECT id, name FROM users",
]
fail = [
    "SELECT * FROM users",
    "SELECT * FROM users WHERE id = 1",
]

[graph]
detect = """
(fact:source @src
  (#match? @src "(?i)\\bSELECT\\s+\\*")
  (#not (#match? @src "(?i)\\bLIMIT\\b")))
"""
