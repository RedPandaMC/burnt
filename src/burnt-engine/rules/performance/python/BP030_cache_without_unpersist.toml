# BP030 - .cache() with no .unpersist() in the same scope
## Rule: BP030
## Description: cache() without unpersist() - potential memory leak
## Severity: warning
## Category: Performance
## Tags: ["pyspark", "memory", "caching"]

[rule]
id = "cache_without_unpersist"
code = "BP030"
severity = "warning"
language = "python"
description = ".cache() with no .unpersist() in the same scope — potential memory leak"
suggestion = "Call .unpersist() when the cached DataFrame is no longer needed"
category = "BestPractice"
tags = ["pyspark", "memory", "caching"]
platform = "all"

[dataflow]
type = "cache_lifecycle"

[tests]
pass = [
    "cached_df = df.cache()\ncached_df.unpersist()",
]
fail = [
    "cached_df = df.cache()\nresult = cached_df.collect()",
]


[graph]
detect = """
(op:Maintenance
  (ast/Call :method "cache")
  (#not (#exists (op:Maintenance (ast/Call :method "unpersist")))))
"""
