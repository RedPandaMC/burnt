[rule]
id = "missing_table_comment"
code = "SDP005"
severity = "info"
language = "python"
description = "SDP table missing comment metadata"
suggestion = "Add table comment for documentation"
category = "SDP"
tags = ["sdp", "dlt", "documentation"]
platform = "databricks"

[tests]
pass = [
    "def foo(): pass",
]
fail = [
    "@dlt.table()\ndef foo(): pass",
]

[graph]
detect = """
(fact:source @src
  (#match? @src "@dlt\\.table\\s*\\("))
"""
